eureka:
  client:
    enabled: false
  application:
    name: clean-architecture-workshop
  cloud:
    config:
      enabled: false
      profile: ${spring.profiles.active}
      label: master
    discovery:
      enabled: false
